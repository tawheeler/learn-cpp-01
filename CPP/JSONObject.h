/*
========================================================================

	An unordered collection of name/value pairs. Its external form
    is a string wrapped in curly braces with colons between the names
    and values, and commas between the values and names. 

    Author: Tim Wheeler
    Contact: timwheeleronline@gmail.com

========================================================================
*/

#include <string>
#include <map>

namespace MysticDave {
    class JSONObject {
        
        private std::map < string, 

    };
}